# Simple GNOME OBS Hotkeys Installer

*Install command:*
```bash
  sh -c "$(curl -fsSL https://raw.githubusercontent.com/z-Eduard005/gnome-obs-hotkeys/main/install.sh)"
```

**You can change hotkeys in default settings app later (saved to custom shortcuts)**